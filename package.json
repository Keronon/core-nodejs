{
  "repository": "https://github.com/Keronon/core-nodejs.git",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^18.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.2"
  },
  "scripts": {
    "build": "rimraf ./build && npx tsc",
    "start": "npm run build && node build/ctrl_root"
  }
}
